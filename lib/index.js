/**
 * useValidator([
 * { fn: () => , text },
 * { fn: () => , text },
 * ])
 */
import { validateFn } from "../lib/validate-fn";
import useValidator from "../lib/use-validator";

export { validateFn, useValidator };
